import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    backgroundColor: "#ddd",
    height: "100%",
    alignItems: "center"
  },
  title: {
    fontSize: 25,
    alignItems: "center",
    fontWeight: "bold",
    color: "red"
  }
});
